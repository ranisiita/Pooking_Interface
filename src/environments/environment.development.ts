export const environment = {
  production: false,
  // Alineado con `dotnet run` por defecto en Booking (launch profile principal).
  apiBaseUrl: 'http://localhost:5225',
  apiTimeoutMs: 30000,
  enableMock: false,
};
